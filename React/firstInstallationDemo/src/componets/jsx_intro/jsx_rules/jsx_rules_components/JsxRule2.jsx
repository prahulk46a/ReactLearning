let JsxRule2=()=> {
    return (
      <div >
          2. If you want to return single element send directly but if you want to return multiple elements then  you should wrap all into one parent element and send that one only.
      </div>
    )
}
export default JsxRule2;